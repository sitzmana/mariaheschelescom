<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64">
  <!-- Monogram. Inherits the page's colour scheme so the tab icon matches the site. -->
  <style>
    rect { fill: #faf8f4; }
    text { fill: #1a1917; }
    @media (prefers-color-scheme: dark) {
      rect { fill: #121110; }
      text { fill: #f2eee6; }
    }
  </style>
  <rect width="64" height="64" rx="12" />
  <text x="32" y="44"
        text-anchor="middle"
        font-family="Iowan Old Style, Palatino Linotype, Georgia, serif"
        font-size="34"
        letter-spacing="-1">MH</text>
</svg>
